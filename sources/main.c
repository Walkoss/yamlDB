#include "gtk/gtk.h"

int main(int argc, char **argv) {

    /*Parser *parser;
    Database *database;

    if (argc >= 2) {
        parser = parserInit(argv[1]);
        parse(parser, &database);
        parserFree(parser);
    }*/

    run(argc, argv);

    return 0;
}